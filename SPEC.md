# Sarah's AI Brief — 제품 명세

> 최종 수정: 2026-08-31 · 상태: Phase 1(정적 사이트)·Phase 2(수집)·Phase 3(요약 생성) 완료, Phase 4(자동 발행) 대기

---

## 1. 목적

AI 관련 보도는 하루에도 수백 건이 쏟아진다. 문제는 정보가 없는 것이 아니라 **무엇이 중요한지 판단할 시간이 없는 것**이다. 대부분의 뉴스레터는 이 문제를 "더 많이 보내는 것"으로 풀려다 실패한다.

Sarah's AI Brief는 반대로 간다. 매일 오전 8시(KST)에 **10건만** 고르고, 각각을 한국어와 영어로 요약하고, 기사마다 "이 기사가 시사하는 점"을 덧붙이고, 10건을 가로질러 읽은 '오늘의 인사이트'를 맨 위에 싣는다.

무료다. 유료 등급도, 광고도, 협찬 기사도, 제휴 링크도 없다.

### 이 제품이 성공했다고 말할 수 있는 조건

- 독자가 브리핑 하나를 읽고 **그날 팀에 공유할 한 문장**을 얻는다.
- 독자가 "왜 이 10건인가"를 물었을 때, **데이터 페이지를 보여주는 것으로 답이 된다**.
- 독자가 요약과 해석을 **혼동하지 않는다**.

---

## 2. 사용자

| 순위 | 사용자 | 상황 | 이 사이트에서 얻는 것 |
|---|---|---|---|
| 1 | 실무자 (기획·개발·전략) | 매일 아침 팀 슬랙에 공유할 거리가 필요하다 | 인사이트 한 문단 + 근거가 되는 기사 10건 |
| 2 | 이중언어 독자 | 한국어로 빠르게 훑고 원문 맥락은 영어로 확인하고 싶다 | 같은 기사의 한/영 요약 + 원문 링크 |
| 3 | 회의적 독자 | "이 큐레이션을 믿을 근거가 있나" | 데이터 페이지의 수집→발행 전 과정 공개 |

3순위 사용자가 이 제품의 설계를 결정한다. 선정 기준을 **적어두는 것**과 그 기준이 실제로 어떻게 작동했는지 **숫자로 보여주는 것**은 다르다. 데이터 페이지는 부가 기능이 아니라 신뢰의 근거다.

### 사용자가 아닌 사람

- 실시간 속보를 원하는 사람 — 하루 한 번, 오전 8시가 전부다.
- 특정 기업·기술만 추적하려는 사람 — 개인화 피드는 제공하지 않는다.
- 원문 전문을 여기서 읽으려는 사람 — 자체 요약만 싣고 원문은 링크로 보낸다.

---

## 3. 원칙

기능 목록보다 이 다섯 가지가 제품을 규정한다. 아래 원칙과 충돌하는 기능은 만들지 않는다.

### P1. 요약과 해석을 물리적으로 분리한다

각 기사는 `요약`과 `이 기사가 시사하는 점` 두 블록으로 나뉜다. 요약에는 **원문에서 확인되는 사실만** 담는다. 시사점은 그 사실에 대한 이 사이트의 판단이다. 독자가 무엇이 보도된 내용이고 무엇이 해석인지 구분할 수 있어야 한다.

→ 설계 반영: 두 블록은 서로 다른 시각적 처리를 받는다. 라벨·배경·좌측 강조선으로 구분하며, 하나의 문단으로 합치지 않는다.

### P2. 선정 과정을 숫자로 공개한다

피드에서 읽어들인 기사 수부터 실린 10건까지의 경로, 매체별 분포, 주제 분포, 용어 누적을 매일 공개한다.

이 숫자는 **AI 업계 전체의 보도량이 아니라 이 사이트가 무엇을 골랐는지에 대한 기록**이다. 특정 매체가 적게 실렸다는 것이 그 매체의 품질을 뜻하지 않는다. 데이터 페이지에 이 문장을 상시 노출한다.

### P3. 원문을 못 읽은 기사는 싣지 않는다

원문 본문을 끝내 확보하지 못한 기사는 예비 후보로 교체한다. 그래도 안 되면 **그날은 10건을 채우지 않고 더 적게 낸다**.

RSS 요약만 보고 쓰는 것은 하지 않기로 한 일이다. 자리를 채우려고 그렇게 하지 않는다. 건수가 미달한 날은 그 사유를 브리핑 상단에 표기한다.

### P4. 한 출처가 지면을 독점하지 않는다

- 한 출처 최대 3건
- 공식 발표(1차 출처)에 최소 2자리 보장
- 같은 사건을 다룬 기사는 하나만 남기고, 나머지는 그 기사에 '같은 사건 다른 매체'로 함께 표시

출처 가중치는 특정 매체를 우대하는 장치가 아니라 **발행량이 많은 매체가 순위를 독점하는 것을 막는 장치**다.

### P5. 같은 링크를 두 번 싣지 않는다

이미 지난 브리핑에 나간 URL은 재선정하지 않는다. 정규화된 URL(쿼리스트링·트래킹 파라미터 제거)을 기준으로 판단한다.

---

## 4. 핵심 기능

| 페이지 | 주소 | 책임 |
|---|---|---|
| 오늘의 브리핑 | `index.html` | 최신 브리핑 전체 렌더 + 구독 CTA + 최근 3일 링크 |
| 날짜별 브리핑 | `brief.html?d=YYYY-MM-DD` | 영구 보관되는 날짜별 고유 주소 |
| 지난 브리핑 | `archive.html` | 월별 그룹 목록. 날짜·요일·건수·인사이트 한 줄, 10건 미달일 배지 |
| 주제별 | `topics.html?t=<topicId>` | 12개 주제 칩 + 선택 주제의 전체 기간 기사 목록 |
| 데이터 | `data.html` | 수집→발행 퍼널, 매체 분포, 주제 분포, 용어 누적 |
| 용어사전 | `glossary.html` | 브리핑에 등장한 용어의 정의·첫 등장일·등장 횟수·관련 브리핑 |
| 소개 | `about.html` | 무엇을 하는 곳인가, 선정 방법, 독립성, 공유·인용 정책 |

### 공통 기능

- **한/영 전환** — 페이지를 복제하지 않고 하나의 데이터에서 두 언어를 렌더한다. `?lang=en`으로 영어판 고유 주소가 생기고, 선택은 브라우저에 저장된다.
- **테마 전환** — 라이트/다크. OS 설정을 기본값으로 쓰되 사용자의 명시적 선택이 양방향으로 이를 덮는다.
- **RSS 구독** — 전문 대신 인사이트와 기사 제목을 담은 피드.
- **주간 회고** — 일요일에는 그 주를 관통한 흐름을 다시 종합한다.

---

## 5. 콘텐츠 규격

### 기사 1건의 구성

| 요소 | 규격 |
|---|---|
| 제목 | 원문 제목의 번역이 아니라 **이 사이트가 다시 쓴 제목**. 한/영 각 1줄, 60자 이내 |
| 출처 | 매체명 + 출처 유형(공식/기술/업계/국내/커뮤니티) |
| 발행 시각 | 원문 발행 시각(UTC 저장, KST 표시) |
| 주제 | 12개 중 1개 |
| 요약 | 한/영 각 2~3문단, 문단당 2~3문장. **원문에서 확인되는 사실만** |
| 시사점 | 한/영 각 1문단, 2~4문장. 판단·전망·맥락 |
| 원문 링크 | 필수. 링크 없는 기사는 싣지 않는다 |
| 같은 사건 다른 매체 | 0~4건. 매체명 + 링크 |
| 용어 | 0~3개. 용어사전 항목과 연결 |

### 문장 규칙

- 요약에서 "~할 전망이다", "~로 보인다" 같은 추측 표현을 쓰지 않는다. 그건 시사점의 몫이다.
- 원문에 없는 수치를 요약에 넣지 않는다.
- 영어 요약은 한국어 요약의 기계 번역이 아니라 **같은 사실을 영어 독자에게 다시 쓴 글**이다.
- 용어사전에 있는 용어는 브리핑 본문에서 처음 나올 때 짧은 동격 설명을 붙인다.

### 오늘의 인사이트

10건을 가로질러 읽은 하나의 흐름. 제목 1줄 + 본문 2~3문단. **10건 중 최소 3건을 근거로 언급**해야 한다. 어느 기사에도 걸리지 않는 일반론은 쓰지 않는다.

### 이 규격을 무엇이 지키는가

요약은 사람이 쓰지 않는다. 기사 1건마다 Claude를 **한 번** 호출해 한/영을 함께 받고, 기사가 다 만들어진 뒤 인사이트를 **한 번 더** 호출한다. 기사별로 나누는 이유는 두 가지다 — 실패한 기사만 다시 부탁할 수 있고, 원문 10편이 한 컨텍스트에 섞이지 않는다.

호출 경로는 둘이고 결과 형식은 같다.

| | 경로 | 인증 | 차이 |
|---|---|---|---|
| 기본 | `claude -p` (Claude Code CLI) | 이미 로그인된 구독 | API 크레딧을 쓰지 않는다. JSON 스키마를 강제할 수 없어 형식을 글로 지시한다 |
| 선택 | Anthropic API | `ANTHROPIC_API_KEY` | `output_config` 로 JSON 스키마를 강제한다 |

어느 쪽이든 아래 검증을 똑같이 통과해야 발행된다. 경로를 바꾸는 것이 규격을 바꾸지 않는다.

**발행 전 사람 검토는 두지 않는다.** 대신 생성 결과를 규격으로 검사한다. 위의 문단·문장 수, 추측 표현, 주제·용어 id, 점수 재현, `{ko, en}` 쌍 완성도, 인사이트의 근거 3건까지 전부 자동으로 본다.

규격을 어긴 기사는 **무엇이 틀렸는지 붙여 다시 만든다**(최대 두 번). 그래도 어기면 그 기사를 빼고 발행한다. 자리를 채우려 규격을 낮추지 않는다 — P3와 같은 태도다. 인사이트가 끝내 실패하면 그날은 발행하지 않는다. 인사이트 없는 브리핑은 이 제품이 아니다.

재시도 횟수는 규격이 아니라 운영 값이다. 늘려도 통과 기준은 그대로이므로 규격을 낮추는 것이 아니다.

오류는 발행 후 정정한다(11절).

---

## 6. 선정 알고리즘

### 6.1 수집 대상 (12개 피드)

| # | 매체 | 유형 | 가중치 |
|---|---|---|---|
| 1 | OpenAI Blog | 공식 | 1.00 |
| 2 | Anthropic News | 공식 | 1.00 |
| 3 | Google DeepMind Blog | 공식 | 1.00 |
| 4 | Meta Engineering | 공식 | 1.00 |
| 5 | MIT Technology Review | 기술 | 0.85 |
| 6 | Ars Technica | 기술 | 0.85 |
| 7 | The Verge | 업계 | 0.80 |
| 8 | TechCrunch | 업계 | 0.80 |
| 9 | VentureBeat | 업계 | 0.80 |
| 10 | 전자신문 | 국내 | 0.75 |
| 11 | ZDNet Korea | 국내 | 0.75 |
| 12 | Hacker News | 커뮤니티 | 0.60 |

#### 피드 보존 깊이 — 국내 피드를 AI 섹션으로 바꿨다

2026-08-31 실측. 각 피드가 한 번에 내주는 항목이 담는 시간 폭:

| 피드 | 항목 | 보존 폭 |
|---|---|---|
| 전자신문 (속보 `Section902.xml`) | 30 | **1.0시간** |
| ZDNet Korea | 30 | **4.1시간** |
| The Verge | 10 | 3일 |
| Ars Technica | 20 | 10일 |
| OpenAI · DeepMind · Meta | 9~1157 | 수백 일 |

국내 두 피드는 발행량이 많아 30개 항목이 몇 시간치밖에 안 된다. **하루 한 번만 수집하면 그 매체의 24시간 중 1~4시간만 보게 된다.** 나머지는 우리가 보기 전에 피드에서 밀려난다.

반대로 공식·기술 매체는 며칠씩 새 글이 없다. 같은 날 실측에서 24시간 창 안의 항목은 12개 피드를 통틀어 4건이었고, 그중 공식은 0건이었다.

**전자신문은 AI 섹션 피드로 바꿨다.** `rss.etnews.com/04.xml`(AI·SW)은 50건을 주고, 같은 날 실측에서 09:00~18:03 KST **약 9시간**을 담았다. 속보 피드의 1.0시간보다 낫고, AI와 무관한 기사가 애초에 들어오지 않아 not-ai 제외(하루 43건)도 함께 줄어든다.

**ZDNet Korea 는 섹션 피드가 없다.** feedburner 전체 피드 하나뿐이고 `news_xml.asp?lstcode=`·`rss.asp`·`zdkorea_ai` 는 모두 404였다. 4.1시간 보존 그대로 간다.

그래도 24시간이 다 덮이지는 않는다. 남은 두 방향은 아직 고르지 않았다:

1. 수집을 하루 여러 번 돌려 항목을 누적한다 (수집과 발행을 분리, 저장소 필요)
2. 창을 24시간보다 넓힌다 (신선도 점수의 정의가 함께 바뀐다)

**그래서 하루 10건이 채워지지 않는 날이 있다.** 그런 날은 P3에 따라 모자란 채로 발행하고 사유를 브리핑 상단에 표기한다. 건수를 채우는 것은 이 제품의 목표가 아니다.

### 6.2 점수식

```
score = 0.40 × 출처가중치
      + 0.35 × 교차보도점수      # 같은 사건을 보도한 독립 매체 수, 4건에서 상한
      + 0.25 × 신선도점수        # 24시간 창 안에서 선형 감쇠
```

세 항 모두 0~1로 정규화한 뒤 100점 만점으로 환산한다. 교차 보도가 알고리즘의 중심인 이유는, 여러 독립 매체가 같은 사건을 동시에 다뤘다는 사실 자체가 **한 편집자의 판단보다 나은 중요도 신호**이기 때문이다.

#### 기준시각과 창

신선도를 계산하려면 "24시간"이 어디에 놓이는지가 정해져야 한다.

- 날짜 T의 브리핑은 **T 08:00 KST**에 나간다. 그 시각이 기준시각이며 UTC로 **T-1 23:00Z**다. KST는 서머타임이 없어 UTC+9 고정이다.
- 창은 기준시각 직전 24시간이다.
- `신선도 = clamp((발행시각 − 창시작) ÷ 24시간, 0, 1)` — 기준시각에 붙은 기사가 1, 창 시작이 0.

예: 2026-08-31 브리핑의 창은 `2026-08-29T23:00Z ~ 2026-08-30T23:00Z`.

#### 점수가 재현되어야 한다

데이터 페이지는 저장된 `scoreParts`에서 점수를 다시 계산해 보여준다(P2). 그러려면 저장된 값으로 계산한 결과가 원래 점수와 **정확히** 같아야 한다.

- 신선도를 **소수 둘째 자리로 반올림한 뒤** 그 값으로 점수를 낸다. 반올림하지 않은 값으로 계산하면 저장된 값에서는 재현되지 않는다.
- 반올림에 `+1e-9` 보정을 넣는다. `0.40×1.0 + 0.35×0.5 + 0.25×0.8`은 부동소수점에서 `0.774999…`가 되어 보정 없이는 78이 아니라 **77**이 나온다.

#### 수집기는 이미 지나간 창만 겨냥한다

아직 열려 있는 창을 겨냥하면 같은 날 두 번 돌렸을 때 결과가 달라진다. 기본 기준일은 **이미 지나간 가장 최근 발행 시각의 날짜**다 — KST 오전 8시 이후면 그날치, 이전이면 전날치.

### 6.3 파이프라인

```
1. 수집        12개 피드에서 항목을 읽는다
2. 시간 필터   최근 24시간 발행분만 남긴다
3. 제외 필터   제품 홍보성 글, 커뮤니티 자기소개 글(Show HN 등)을 뺀다
4. 중복 묶음   같은 사건을 다룬 기사를 하나로 묶고 대표 기사를 정한다
5. 원문 확보   본문을 읽는다. 실패한 기사는 후보에서 제외한다  (→ P3)
6. 점수 산정   6.2의 식으로 순위를 매긴다
7. 상한 적용   한 출처 3건 제한, 공식 발표 2자리 보장          (→ P4)
8. 중복 배제   지난 브리핑에 나간 URL을 뺀다                   (→ P5)
9. 확정        상위 10건. 후보가 모자라면 모자란 채로 발행한다  (→ P3)
```

각 단계에서 몇 건이 걸러졌는지는 데이터 페이지에 그대로 공개한다.

**구현상의 순서 두 가지는 위 번호와 다르다.** 결과는 같고 이유가 있다.

| 조정 | 이유 |
|---|---|
| 8단계(이미 실린 URL)를 3단계에서 처리 | 뒤에서 빼면 10자리에 구멍이 난다. 이미 실은 기사를 다시 크롤링할 이유도 없다. 6.4의 제외 규칙에 이미 들어 있는 항목이다 |
| 6단계(점수)를 5단계(원문 확보)보다 먼저 | P3의 "실패한 기사는 예비 후보로 교체한다"를 지키려면 점수 순서대로 본문을 받아야 한다. 점수를 매겨야 그 순서가 정해진다 |

#### 4단계 상세 — 같은 사건을 어떻게 알아보는가

교차 보도는 점수의 35%다. 이 판정이 틀리면 순위 전체가 틀리므로 6.2의 점수식만큼 정확히 적어 둔다.

**준비.** 제목에서 매체명 꼬리표(`| TechCrunch`, `- The Verge`)를 떼고, 소문자·구두점 정리 후 토큰으로 나눈다. 한글은 조사를 떼어 `엔비디아가`와 `엔비디아의`가 같은 토큰이 되게 한다. 불용어와 두 자 미만은 버린다.

**엔티티 사전이 한↔영 다리다.** `엔비디아, 블랙웰 공급 확대`와 `Nvidia expands Blackwell supply`는 토큰이 **하나도** 겹치지 않는다. 회사·모델·칩·인물의 한영 별칭을 하나의 이름으로 모아 두면 두 기사의 엔티티가 `{nvidia, blackwell}`로 같아진다.

**묶음 판정** — 둘 중 하나면 같은 사건으로 본다.

| | 조건 |
|---|---|
| ① 제목 유사도 | 토큰 Jaccard **≥ 0.45** |
| ② 엔티티 + 시간 | 공유 엔티티 **≥ 2개** **그리고** 발행 시각 **12시간 이내** |

②가 언어를 넘는 교차 보도를 잡는 장치다. 엔티티 1개만 겹치는 것으로는 묶지 않는다 — `Nvidia 실적`과 `Nvidia·삼성 협력`이 한 사건이 되어 버린다.

**대표 기사** = 출처 가중치가 가장 높은 기사. 동률이면 **먼저 보도한 쪽**.

**교차 보도 수 = 독립 매체 수.** 한 매체가 같은 사건을 두 건 써도 하나로 센다. 자기 기사로 자기 중요도를 올릴 수 없어야 한다.

**같은 매체의 같은 사건도 묶는다.** P4는 "같은 사건을 다룬 기사는 하나만 남긴다"이지 매체를 가리지 않는다. 실측에서 한 매체가 같은 사건을 두 각도로 쓴 기사가 10자리 중 두 자리를 먹었다.

**한계.** 사전에 없는 신규 고유명사는 놓친다. 연쇄 병합(A~B, B~C면 A·B·C가 한 묶음)이 일어날 수 있다. 그래서 유사도 0.30~0.45 구간의 **묶이지 않은 쌍을 보고서에 따로 낸다** — 사전을 어디에 보강해야 하는지 드러내기 위해서다.

### 6.4 제외 규칙

- 제품 홍보·보도자료 성격이 명백한 글
- 커뮤니티 자기소개 글 (Show HN, 개인 프로젝트 런칭 등)
- 원문 본문 확보 실패
- 이미 지난 브리핑에 실린 URL
- AI와 직접 관련 없는 일반 기술 기사

#### AI 관련성 판정 — 표제가 기준이다

본문이나 요약 어디든 'AI'가 한 번 스치면 통과시키는 방식은 쓰지 않는다. 실측에서 그 방식은 **전기설비 수주 기사**('AI 데이터센터 수요' 언급), **게임 업데이트 기사**('에이전트'가 캐릭터 이름), **항공우주 M&A 기사**를 모두 통과시켰다.

하루 10건을 고르는 브리핑이 실을 것은 AI *에 관한* 기사이고, 그런 기사는 표제가 그렇다고 말한다.

| 통과 조건 | |
|---|---|
| 제목에 확실한 AI 용어 | 인공지능, LLM, ChatGPT, 생성형, OpenAI, 딥러닝 … |
| 제목에 인접 용어 + 요약에 확실한 용어 | 인접 용어 = 반도체, 로봇, 데이터센터, 자율주행, 에이전트 … |

공식 4개 피드는 이 검사를 면제한다. 전부 AI 기업의 자체 발표이기 때문이다.

홍보성 판정도 **제목만** 본다. 요약문의 '개최·협약' 같은 말로 걸었더니 휴머노이드 생태계 기사와 헬스케어 AI 도입 기사가 함께 죽었다.

### 6.5 원문 확보

P3이 걸린 단계다. 여기서 실패한 기사는 후보에서 빠지고 그만큼 그날 브리핑이 짧아진다.

#### 추출 방법

앞의 것이 성공하면 뒤는 시도하지 않는다.

| | 방법 |
|---|---|
| 1 | JSON-LD의 `articleBody` (`@type`이 `NewsArticle`·`Article`·`BlogPosting` 등) |
| 2 | `<article>` / `<main>` 안의 `<p>` |
| 3 | 문서 전체의 `<p>` 중 충분히 긴 것 (밀도 폴백) |

판정 전에 `script`·`style`·`nav`·`footer`·`aside` 등을 걷어낸다. 저작권 문구와 `입력 :/수정:` 타임스탬프 같은 장식 문단도 버린다 — 밀도 폴백이 이런 것을 본문 첫 문단으로 삼는 일이 실제로 있었다.

**본문이 300자 미만이면 확보 실패**로 보고 후보에서 뺀다.

#### 자체 구현으로 충분한가 — 실측으로 답한 것

12개 매체에서 각 3건씩 36건을 받아 봤다 (2026-08-31).

| | |
|---|---|
| 성공 | **30/36 = 83%** |
| 본문 길이 중간값 | 5,728자 |
| **파싱 실패** | **0건** |

실패 6건은 전부 접근 거부(403·429)였고 **추출 실패는 한 건도 없었다.** 병목은 추출기가 아니라 접근이다. 따라서 `@mozilla/readability` 같은 라이브러리를 들이지 않는다 — 들여도 이 6건은 그대로 실패한다.

이 판정을 근거와 함께 남겨 둔다. 나중에 추출 품질이 의심되면 같은 방식으로 다시 재면 되고, 근거 없이 라이브러리부터 넣는 일은 하지 않는다.

#### 접근이 막힌 매체

원문을 받지 못하는 이유가 우리 쪽 문제가 아닌 경우가 있다. 2026-08-31 실측:

| 매체 | 응답 | 성격 |
|---|---|---|
| OpenAI Blog | HTTP 403 (Cloudflare) | 봇 차단 |
| VentureBeat | HTTP 429 (Vercel) | 단건 요청에도 거부 |

**우회하지 않는다.** 상대가 막아 둔 것을 뚫는 것은 이 사이트가 할 일이 아니다. P3에 따라 해당 매체 기사는 싣지 않고, 그만큼 그날 건수가 줄어든 사실을 데이터 페이지에 남긴다.

---

## 7. 데이터 계약

파이프라인(Phase 2~3)이 생성할 산출물의 형식을 여기서 고정한다. 사이트는 이 형식만 알면 되고, 파이프라인은 이 형식만 지키면 된다.

데이터는 JSON이 아니라 **전역 객체에 대입하는 `.js` 파일**로 둔다. `fetch()`는 `file://`에서 CORS로 차단되므로, 파일을 직접 열었을 때 화면이 비는 것을 막기 위한 선택이다.

### `data/meta.js`

```js
window.SAB = window.SAB || {};
SAB.meta = {
  site: { title, tagline: {ko,en}, publishTime, timezone },
  sourceTypes: [{ id, label:{ko,en}, weight }],
  feeds:  [{ id, name, type, url, home }],
  topics: [{ id, label:{ko,en}, description:{ko,en} }]   // 12개
};
```

### `data/briefs.js` — 최신순 정렬

```js
SAB.briefs = [{
  date: "2026-08-31",
  weekday: {ko,en},
  type: "daily" | "weekly",
  insight: { title:{ko,en}, body:{ko:[문단], en:[문단]} },
  note: {ko,en} | null,               // 10건 미달 사유 등, 없으면 null
  funnel: {
    collected,      // 1. 수집
    window24h,      // 2. 시간 필터 통과
    excluded,       // 3. 제외 필터로 뺀 수
    deduped,        // 4. 중복 묶음 후 남은 수
    fetchFailed,    // 5. 원문 확보 실패
    scored,         // 6. 점수 산정 대상
    published       // 9. 최종 발행
  },
  articles: [{
    id, rank,
    title:{ko,en},
    source, sourceType, url, publishedAt,   // publishedAt은 ISO8601 UTC
    topic, score, scoreParts:{weight,cross,fresh},
    crossRefs:[{source,url}],
    summary:{ko:[문단], en:[문단]},
    implication:{ko,en},
    terms:[glossaryId]
  }],
  weekly: {                                  // type === "weekly" 일 때만
    sections:[{ title:{ko,en}, body:{ko,en}, refs:[{date, articleId}] }]
  }
}];
```

### `data/glossary.js`

```js
SAB.glossary = [{
  id, term:{ko,en}, definition:{ko,en},
  firstSeen: "2026-08-12",     // 첫 등장 브리핑 날짜
  count                        // 누적 등장 횟수
}];
```

### 중간 산출물 (Phase 2 → Phase 3)

수집기가 만들고 요약기가 먹는 형식이다. 사이트는 이 파일을 읽지 않는다.

```
runs/<date>/candidates.json   후보 + 원문 본문        (git 제외)
runs/<date>/funnel.json       퍼널 숫자만
runs/<date>/report.txt        사람이 읽는 보고서 (Phase 3 결과가 뒤에 덧붙는다)
runs/<date>/brief.json        Phase 3 이 만든 브리핑. 발행 여부와 무관하게 남는다
runs/<date>/raw/…             원본 XML·HTML           (git 제외)
```

```js
{
  date, generatedAt, anchor, windowStart,
  funnel: { … },                 // 위 briefs.js 의 funnel 과 필드명이 같다
  excludedBy: { 'not-ai': n, promo: n, 'community-self': n, 'already-published': n },
  extraction: [{ source, tried, ok, via:{jsonld,article,density}, failed:{…} }],
  feedStatus: [ … ],
  shortfall: { missing, fetchFailed, scored, reason } | null,
  primaryGuarantee: { required, achieved, availableInPool, swaps:[…] },
  candidates: [{
    rank, url, canonicalUrl,
    sourceTitle,                 // 원문 제목 그대로. Phase 3 이 다시 쓴다
    source, sourceType, publishedAt,
    score, scoreParts:{weight,cross,fresh},
    crossRefs:[{source,url}],
    clusterSize, clusterWhy,     // 왜 같은 사건으로 묶였는지 (P2)
    text, textChars, extractedVia
  }]
}
```

- `topic`은 Phase 2가 정하지 않는다. 점수에 쓰이지 않고, 12개 중 하나를 고르는 일은 본문을 읽는 Phase 3이 더 잘한다.
- `text`는 기사 원문이다. **저장소에 커밋하지 않는다** (11절 저작권). `runs/`는 `.gitignore`에 있다.
- 같은 입력 XML이면 항상 같은 후보가 나온다. Phase 2에는 LLM이 없다. 그 경계 덕분에 '잘못 골랐다'와 '잘못 요약했다'를 따로 디버깅할 수 있다.

### 불변 규칙

- `date`는 브리핑의 고유 키다. 한 날짜에 두 개의 브리핑은 없다.
- `articles[].id`는 `<date>-<rank 2자리>` 형식이며 발행 후 변경하지 않는다.
- 발행된 브리핑은 **삭제하지 않는다**. 오류가 발견되면 해당 날짜 페이지를 수정하고 수정 사실을 남긴다.
- 모든 사용자 노출 문자열은 `{ko, en}` 쌍을 갖는다. 한쪽만 있는 필드는 허용하지 않는다.

---

## 8. 제약

### 기술 제약

| 제약 | 이유 |
|---|---|
| 빌드 도구 없음 (순수 HTML/CSS/JS) | 의존성이 썩지 않는다. 3년 뒤에도 그냥 열린다 |
| `file://`에서도 동작해야 함 | 서버 없이 확인 가능. 데이터를 `.js`로 두는 이유 |
| 페이지는 100% 정적 | 방문자 요청 때마다 서버가 하는 일이 없다. 서버가 죽어도 사이트는 살아 있다 |
| 외부 폰트는 실패해도 무방 | Pretendard CDN 실패 시 시스템 폰트로 저하, 레이아웃 유지 |
| 최신 2개 버전 브라우저 | 트랜스파일 없이 최신 문법 사용 |
| 사이트 코드에 라이브러리 0개 | 차트 포함 전부 자체 구현. 서버 코드에는 적용되지 않는다 |

### 아키텍처 경계

백엔드를 두되, **역할을 두 가지로 못 박는다.**

| 서버가 하는 일 | 서버가 하지 않는 일 |
|---|---|
| 하루 한 번 12개 피드를 수집하고 6.3 파이프라인을 돌린다 | 페이지를 렌더링한다 |
| 요약·시사점을 생성해 `data/*.js`를 만들고 배포한다 | 방문자 요청마다 DB를 조회한다 |
| 구독 신청을 접수하고 확인 메일과 브리핑을 보낸다 | 기사 목록을 API로 서빙한다 |

방문자 수가 늘어도 서버 비용은 늘지 않는다. 수집·요약 비용은 **하루 1회로 고정**이고, 페이지는 CDN이 나른다. 서버 장애는 그날 브리핑이 늦어지는 문제이지 사이트가 내려가는 문제가 아니다.

RSS 수집을 브라우저에서 하지 않는 이유도 같다. 언론사 RSS는 CORS를 열어주지 않고, 설령 프록시로 뚫더라도 방문자마다 12개 피드를 긁고 LLM을 호출하는 구조가 된다. 수집·요약은 **미리 한 번** 돌아야 하는 작업이다.

### 수집 예절

우리는 남의 서버에서 기사를 받아 오는 쪽이다. 어떻게 받는지도 규격의 일부다.

- **UA로 자신을 밝힌다** (`SarahsAIBrief/1.0`). 누가 긁고 있는지 상대가 알 수 있어야 한다.
- **`robots.txt`를 지킨다.** origin당 한 번 읽어 캐시하고, 와일드카드 `*`와 `$` 앵커를 처리하며, 우리 UA 전용 그룹이 `*` 그룹보다 우선한다. 최장 일치 규칙을 따르고 길이가 같으면 `Allow`가 이긴다.
- **호스트당 최소 1.5초 간격**, 전체 동시 4건. 한 서버에 몰아치지 않는다.
- **4xx는 재시도하지 않는다.** 주소가 틀렸다는 뜻이므로 다시 두드릴 이유가 없다. 5xx와 연결 예외만 백오프를 두고 3회까지 본다.
- **403·429를 우회하지 않는다.** 상대가 막아 둔 것을 뚫는 것은 이 사이트가 할 일이 아니다. P3에 따라 건수 감소로 처리한다.
- **받아 온 본문은 저장소에 커밋하지 않는다.** 중간 산출물(`runs/`)에만 두고 `.gitignore`로 막는다.

`robots.txt` 해석은 대충 하면 안 된다. 초기 구현에서 `Disallow: */trackback/`의 `*` 앞을 잘라 접두사로 쓴 탓에 빈 접두사가 모든 경로에 매칭됐고, TechCrunch·Ars Technica·MIT Technology Review 세 매체가 통째로 차단됐다. 추출 성공률이 56%로 찍혔다가 고친 뒤 83%가 됐다.

### 콘텐츠 제약

- 기사 원문의 저작권은 각 언론사에 있다. **자체 요약과 시사점만 싣고 원문 전문은 게재하지 않는다.**
- 현재 저장소의 데이터는 **구조 확인용 샘플**이다. 실제 보도된 기사가 아니며, 사이트 상단에 이를 명시한다. 출처 링크는 가짜 기사 URL 대신 각 매체 홈으로 연결한다.

### 개인정보 제약

- 수집하는 개인정보는 **이메일 주소 하나**뿐이다.
- 브리핑 발송 외의 목적으로 쓰지 않으며 제3자에게 제공하거나 판매하지 않는다.
- 추적 스크립트, 광고 픽셀, 제휴 링크를 넣지 않는다.
- Phase 1에서는 구독 폼이 아무것도 전송하지 않는다. 폼 UI와 안내 문구만 두고, 연동 지점을 코드 주석으로 표시한다.

### 독립성 제약

- 어떤 매체나 기업으로부터도 대가를 받지 않는다.
- 광고, 협찬 기사, 제휴 링크가 없다.
- 특정 매체를 고정적으로 우대하지 않는다.

---

## 9. 비목표

이번에도, 앞으로도 하지 않는 것:

- 실시간 속보 — 하루 한 번 오전 8시
- 댓글·커뮤니티 기능
- 개인화 피드 / 관심사 기반 추천
- 유료 등급, 멤버십, 후원
- 기사 원문 전재
- 소셜 로그인, 사용자 계정
- 앱 (웹만)

---

## 10. 로드맵

| Phase | 내용 | 완료 조건 |
|---|---|---|
| **1. 정적 사이트** *(완료)* | 7개 페이지, 한/영 전환, 샘플 데이터 4일치 | 서버 없이 `index.html`을 열어도 렌더되고, 7개 페이지 콘솔 에러 0건 |
| **2. 수집·점수화** *(완료)* | Node 스크립트가 12개 피드를 읽고 6.3 파이프라인을 실행해 후보 목록과 퍼널 숫자를 생성 | `npm run collect`가 후보 JSON과 퍼널 수치를 뱉는다 |
| **3. 요약 생성** *(완료)* | Claude로 한/영 요약·시사점·인사이트 생성, `data/briefs.js` 갱신 | 사람 손 없이 하루치 `briefs.js`가 만들어진다 |
| **4. 자동 발행** *(진행 중)* | 스케줄러가 매일 08:00 KST에 2~3단계를 실행하고 이어서 배포 | 사람 개입 없이 3일 연속 자동 발행 성공 |
| **5. 구독 발송** | 구독 접수 API + 이메일 저장 + 발송. 영어판 구독자에게는 영어로 발송 | 확인 메일과 브리핑이 구독 언어에 맞게 도착한다 |

Phase 2 이후에도 **사이트 코드는 고치지 않는다**. 7절의 데이터 계약이 그 경계다.

### 왜 07:40 이 아니라 08:00 에 시작하는가

날짜 T 의 브리핑은 T 08:00 KST 를 기준시각으로 삼고 그 직전 24시간을 본다(6.2). 수집기는 **이미 닫힌 창만** 겨냥한다 — 열려 있는 창을 겨냥하면 같은 날 두 번 돌렸을 때 결과가 달라지기 때문이다.

07:40 에 시작하면 T 의 창이 아직 20분 남아 있어 그 규칙을 어기게 된다. 그래서 08:00 에 시작하고, 생성에 걸리는 몇 분만큼 늦게 반영된다. **결정적인 결과가 정시 발행보다 우선한다** — 같은 날 다시 돌려도 같은 브리핑이 나와야 P2의 공개 숫자가 의미를 갖는다.

---

## 11. 저작권 · 공유 정책

### 이 사이트의 글

사내 슬랙, 팀 채널, 사내 뉴스레터, 회의 자료에 그대로 옮겨도 된다. 따로 알릴 필요도 없다. 매일 아침 각자의 자리에서 그렇게 쓰이라고 만든 브리핑이다.

뉴스레터·블로그·영상의 소재로 써도 된다. **출처와 링크만 남겨 달라.** 하루치 전체를 그대로 옮겨 다른 곳에서 다시 발행하는 것만 삼가 달라.

### 기사 원문

원문의 저작권은 각 언론사에 있다. 이 사이트는 자체 요약과 시사점만 싣고 원문 전문은 게재하지 않으므로, 위 허용 범위는 **이 사이트가 쓴 글에 한한다.**

### 오류 정정

오류를 발견하면 알려 달라. 확인 후 해당 날짜 페이지를 수정한다. 브리핑 자체는 삭제하지 않는다.
